# wedding

이수영 · 이정은 모바일 청첩장.

- **공개 URL:** https://suyoung-lee.github.io/wedding/
- 이 URL은 종이 청첩장 QR코드에 인쇄되므로 **절대 변경 금지**
  (레포명 `wedding`, 계정명 `suyoung-lee`, 브랜치 `main`, Pages 경로 `/` 전부 고정)

## 예식 정보

| | |
|---|---|
| 일시 | 2026년 12월 12일 (토) 오후 4시 |
| 장소 | 세인트메리엘 1층 메리엘홀 |
| 주소 | 서울 강남구 논현로79길 72 올림피아센터 빌딩 (역삼동 828-10) |
| 연락처 | 02-538-3300 |

## 배포

`main` 에 push 하면 GitHub Pages가 자동 배포합니다. 백업 미러에도 함께 밀어두세요.

```bash
git push origin main && git push backup main
```

## 로컬 미리보기

```bash
python -m http.server 8137
```

`file://` 로 열면 상대경로 이미지가 안 잡히니 반드시 서버로 여세요.

## 사진 교체 / 추가

원본은 `imgs/` 에 두고, **`imgs/` 는 `.gitignore` 로 제외돼 있습니다.**
public 레포라서 원본을 커밋하면 원본 해상도가 URL로 공개되고 아래 확대 방지가 무의미해집니다.

1. `imgs/` 에 사진을 넣습니다
2. `build_assets.py` 의 `ORDER` 목록을 수정합니다 (촬영 세트별로 묶여 있습니다)
3. `python build_assets.py` — `assets/` 에 웹용 이미지가 생성됩니다
4. 출력 하단의 `<div class="cell">...` 블록을 `index.html` 갤러리에 붙여넣습니다

> **가로 사진 주의** — 2열 격자에서 가로 사진은 2칸을 차지합니다.
> 앞에 오는 세로 사진 개수가 **짝수**여야 빈칸이 안 생깁니다.
> 현재 가로 사진은 7번이고 앞에 6장이라 정확히 맞습니다.

## 사진 확대 방지

모바일 청첩장 관행대로 사진을 확대할 수 없게 해두었습니다.

- viewport `maximum-scale=1, user-scalable=no`
- `touch-action` — 더블탭 확대 / 핀치 줌 차단
- JS로 iOS `gesture*` 이벤트, 두 손가락 `touchmove`, 더블탭, `ctrl`+휠 취소
  (iOS Safari는 `user-scalable=no` 를 무시하므로 JS가 필요합니다)
- 사진 `pointer-events:none` + `contextmenu` 취소 → 길게 눌러 저장 차단
- 갤러리에 라이트박스/확대 링크 없음
- 웹용 이미지는 가로 900px로 리사이즈 (원본 비공개)

**한계:** 스크린샷 후 사진 앱에서 확대하는 것은 막을 수 없습니다.
약도는 읽어야 하므로 예외로 두어, 누르면 새 탭에서 크게 볼 수 있습니다.

## 남은 작업

- [ ] 신부측 계좌번호 3개 (신부 이정은 / 아버지 이희수 / 어머니 임명자)
- [ ] 실제 폰에서 계좌 **복사 버튼** 동작 확인
      (특히 카카오톡 인앱 브라우저 — clipboard API가 막히면 구형 방식으로 넘어가고,
       그것도 막히면 "길게 눌러 복사" 안내가 뜹니다)
- [ ] 실제 폰에서 사진 확대가 정말 안 되는지 확인 (iOS / Android 각각)
- [ ] 카카오톡으로 링크 보내서 미리보기 카드(`assets/og.jpg`) 확인
- [ ] 네이버지도 / 카카오맵 버튼이 세인트메리엘로 제대로 가는지 확인
